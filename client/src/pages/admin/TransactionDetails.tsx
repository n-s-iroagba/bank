import {Card } from "react-bootstrap";

const  TransactionDetails = ({ clientId }: { clientId: number }) => {
   return(
    <Card>
Transaction details
{'ADD ISAdmin'}
    </Card>
   )
  }
  export default TransactionDetails